--- AtlasDashboard.tsx
+++ AtlasDashboard.tsx
@@ -230,8 +230,8 @@
                   contentStyle={{ backgroundColor: "#1e1e1e", border: "1px solid #333" }}
                   labelStyle={{ color: "#d1d5db" }}
                   formatter={(val: number) => [val, "Starts"]}
-                  labelFormatter={(_label: unknown, payload: any) => {
-                    const typedPayload = payload as { payload?: { pathLabel?: string, starts?: number } }[];
+                  labelFormatter={(_label: unknown, payload: readonly any[]) => {
+                    const typedPayload = payload as readonly { payload?: { pathLabel?: string, starts?: number } }[];
                     return typedPayload?.[0]?.payload?.pathLabel
                       ? `${typedPayload[0].payload.pathLabel} — بدايات: ${typedPayload[0].payload.starts}`
                       : _label as string;
@@ -270,8 +270,8 @@
                   contentStyle={{ backgroundColor: "#1e1e1e", border: "1px solid #333" }}
                   labelStyle={{ color: "#d1d5db" }}
                   formatter={(val: number) => [val, "Completed Tasks"]}
-                  labelFormatter={(_label: unknown, payload: any) => {
-                    return (payload as { payload?: { date?: string } }[])?.[0]?.payload?.date ?? _label as string;
+                  labelFormatter={(_label: unknown, payload: readonly any[]) => {
+                    return (payload as readonly { payload?: { date?: string } }[])?.[0]?.payload?.date ?? _label as string;
                   }}
                 />
                 <Bar dataKey="completedTasks" fill="#10b981" radius={[4, 4, 0, 0]} />
